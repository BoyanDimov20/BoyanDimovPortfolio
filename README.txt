To run the project you should start development server:

1) Go to directory `src/client`
2) Open terminal in current directory
3) Run `npm install`
4) Run `npm start`

Application should start on http://localhost:3000/